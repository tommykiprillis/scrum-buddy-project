<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Scrum Buddy</title>
  </head>

  <body>
    <% if (locals.message) { %>
    <h1>Incoming Message: <%= message %></h1>
    <% } else { %>
    <h1>Enter the task below 👇</h1>
    <% } %>
    <form action="/submit" method="POST">
      <input type="text" name="taskTitle" placeholder="Title" />
      <input type="text" name="tastDescription" placeholder="Description" />
      <input type="submit" value="OK" />
    </form>
  </body>
</html>
